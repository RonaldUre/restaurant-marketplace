package com.ronaldure.restaurantmarketplace.restaurant_marketplace.restaurant.application.view;

/** Minimal view for select inputs (platform). */
public record RestaurantForSelectView(Long id, String name) {}